import React from 'react';

export const FirebaseConfigError: React.FC = () => {
    // This component is no longer used as Firebase initialization is now handled
    // with a placeholder to prevent crashes in local environments.
    return null;
};